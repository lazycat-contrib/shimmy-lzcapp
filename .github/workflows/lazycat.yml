name: Publish LazyCat application

on:
  schedule:
    - cron: "31 3 * * *"
  workflow_dispatch:
    inputs:
      force_image_check:
        description: Process the selected image even when the same app version is under review
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  pull-requests: write

jobs:
  lazycat:
    uses: ca-x/lazycat-github-action/.github/workflows/lazycat.yml@v1
    with:
      operation: ${{ inputs.force_image_check && 'check' || 'auto' }}
      config: .github/lazycat-action.yml
      versioned-release-asset: true
    secrets:
      LZC_API_TOKEN: ${{ secrets.LZC_API_TOKEN }}
      APPSTORE_URL: ${{ secrets.APPSTORE_URL }}
      APPSTORE_TOKEN: ${{ secrets.APPSTORE_TOKEN }}
      APP_ID: ${{ secrets.APP_ID }}
      PRIVATE_STORE_GROUP_CODES: ${{ secrets.PRIVATE_STORE_GROUP_CODES }}
